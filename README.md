# TERYT webservices client library for PHP

## Current status

There is a functional SOAP client class. Everything else is in early stages of development.

## Running tests

Prepare `bootstrap.php` from `bootstrap.sample.php` and run PHPUnit:

```
phpunit
```
